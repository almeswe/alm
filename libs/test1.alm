import io;
import file;

/*func fill_array(integer arrSize) : integer[]
{
	integer[] arr = integer[arrSize];
	integer i = 0;
	while(i < arrSize)
	{
		arr[i] = arrSize + i*i;
		i += 1;
	}
	return arr;
}

func main(integer arrSize) : void
{
	integer[] arr = fill_array(arrSize);
	print_array(arr,false);
	println("---------------------------------------------");
	print_array(selection_sort(arr),false);
	
}*/

/*func main() : integer
{	
	while(true)
	{
		string input = read("Введите размер массива: ");
		if (input == "q") 
			{ return 0; }
		if (IsCorrectNumberInput(input,2))
			{main(toint(input));}
		else 
			{println(Concat("Неверный ввод числа: ",input));}
	}


	read();
	return 0;
}*/

func main() : integer
{
	string[] lines;
	string path = "C:\\Users\\Almes\\Dessktop\\DebugLog.txt";

	if (Exists(path))
	{

		lines = ReadAllLines(path);
		integer i = 0;	
		while (i < len(lines))
		{
			println(lines[i]);i+=1;
		}

	}
	else 
		{println("Bad path.");}

	
	read();
	return 0;
}


































